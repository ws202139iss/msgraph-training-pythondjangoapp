apiVersion: v1
kind: Service
metadata:
    name: "flasklab2-cdc5"
    labels:
        app: "flasklab2-cdc5"
spec:
    type: LoadBalancer
    ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
    selector:
        app: "flasklab2-cdc5"